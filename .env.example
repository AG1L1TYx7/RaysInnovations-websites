# Database Configuration
# PostgreSQL connection string
# For local PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/rays_innovations

# For Neon serverless PostgreSQL (recommended):
# DATABASE_URL=postgresql://username:password@host.neon.tech/dbname?sslmode=require

# Example with actual values (replace with your own):
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/rays_innovations

# Server Configuration
PORT=5000
NODE_ENV=development

# Session Configuration (optional - will be auto-generated if not provided)
# SESSION_SECRET=your-super-secret-session-key-here

# Database-specific variables (automatically extracted from DATABASE_URL)
# These are set automatically by the Neon serverless package
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=your_password
# PGDATABASE=rays_innovations

# Frontend Configuration (if needed)
# VITE_API_URL=http://localhost:5000

# Optional: Email Configuration (for future email features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# EMAIL_FROM=noreply@raysinnovations.com

# Optional: Cloud Storage (for future file uploads)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=rays-innovations-uploads

# Optional: Analytics (for future analytics features)
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Optional: External APIs (for future integrations)
# OPENAI_API_KEY=sk-...
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...